<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
            https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.4.xsd">

    <changeSet id="profile" author="troinin" runOnChange="true">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="profile"/>
            </not>
        </preConditions>

        <createTable tableName="profile">
            <column name="profile_id" type="uuid">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="username" type="nvarchar2(64)">
                <constraints nullable="false" unique="true"/>
            </column>
            <column name="email" type="nvarchar2(64)">
                <constraints nullable="false" unique="true"/>
            </column>
            <!-- temporary -->
            <column name="secret_key" type="nvarchar2(256)">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <rollback>
            <dropTable tableName="profile"/>
        </rollback>
    </changeSet>

</databaseChangeLog>